- add baseline detection and max psi reporting to calculations log 
    (the code for this in in evaluation window; method named score)
- find a way to prevent the tkcal.DateEntrys from filling with a default value
- rename testhandler close method
- entry validation (dont accept chars where we need numbers)
- move project defaults into a JSON/YAML
- modal calculator (port over)

------- 
script to port over old results
-------


- reportsettings implement colorcycle and legend position
- other pump commands (flowrate etc.) 
    - add this to project parameters?
- streamline method calls in EvaluationWindow 
- make plot in eval window interactive (need to use pyplot instead of 'fig, ax' ?)
- exceptionhandling (missing files?)
- packaging (exe, etc?)